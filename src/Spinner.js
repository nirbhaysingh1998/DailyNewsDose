import React from 'react';
import spinner from './Images/spinner.gif'
const Spinner = () => {
    return (
        <div className='flex  justify-center'>

            <img className='block' src={spinner} alt='no spinner' />     </div>
    );
}

export default Spinner;
